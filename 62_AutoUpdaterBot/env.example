# AutoUpdaterBot Environment Configuration
# Copy this file to .env and fill in your actual values

# GitHub Personal Access Token (Optional, but recommended for private repos and higher API limits)
# GITHUB_TOKEN=ghp_your_personal_access_token

# Default branch name to use if not specified in CLI
# DEFAULT_BRANCH=main

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO
